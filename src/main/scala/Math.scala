object Math {

  def plus(x: Int, y: Int): Int ={
    return x + y;
  }
  def minus(x: Int, y: Int): Int ={
    return x - y;
  }
  def into(x: Int, y: Int): Int ={
    return x * y;
  }
  def divided(x: Int, y: Int): Int ={
    return x / y;
  }
}
